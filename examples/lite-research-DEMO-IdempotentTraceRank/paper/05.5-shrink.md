# STEP 5½ — SHRINK pass

Classification of every step in the draft (delete / move / leave):

| Step | Class | Action | Reason |
|---|---|---|---|
| L1 proof (factorization) | boring + important | LEAVE inline | 4 lines; an appendix would be longer than the proof |
| L2 proof (cancellation) | boring + important | LEAVE inline | 2 lines |
| L3 proof (crux) | important | LEAVE inline | the paper's content |
| L4 proof (trace comm.) | boring + elementary | LEAVE inline (candidate for deletion) | kept because R2's one-line chain cites it directly; deleting would gap the chain (statement-integrity rule) |
| R2 proof | important | LEAVE | one displayed line |
| Thm proof | important | LEAVE | 4 lines, carries the hinge |
| S4 audit row, S14, case-split rows | — | ALREADY ABSORBED at Step 4 | never drafted; recorded in `04-lemmas.md` |

**Deleted steps: none** → `deleted-steps.md` not created (nothing to preserve).
**Moved to appendix: none** (no appendix exists; nothing qualifies).

GATE 5½: (a) latexmk clean after the pass (no edits made — trivially) ✓; (b) two-way
ref audit: no appendix, every `\ref` resolves (latexmk log: no undefined references) ✓;
(c) page-count delta: 3 → 3 (the pass may not grow the text; it did not) ✓;
(d) statement-integrity: theorem/proposition/lemma statements byte-identical before and
after (no edits) ✓.
