# STEP 6 — abstract, title (final), outlook + the summary-layer audit trail

- **Abstract** written last, from the finished content; carries: the identity, the
  mechanism (A = BC, CB = I_r), the characteristic-free form, the char-0 rider AND the
  order-on-ℝ rider, the honest verification clause (displayed identity kernel-checked
  end to end, by an independent route, printed proof formalized only at its crux).
- **Title confirmed** against content: *Trace and Rank of Real Idempotent Matrices*
  (unchanged from Step 3).
- **Outlook**: two genuinely open formal items — the printed factorization route
  beyond its crux, and the characteristic-free proposition over a general field.

## Audit trail (gates 5(d) + 6)

- **Round 1: FAIL / FAIL.** 9 gaps + 4 nitpicks — the serious ones all
  summary-layer: the abstract claimed end-to-end checking of the whole theorem
  (uniqueness + bound clauses are outside the certificate), dropped the
  independent-route rider and the order-on-ℝ rider; §Verification said "no live
  objections" (eight nitpicks were live at closure) and "every computationally
  testable row"/"every experiment script" (both checkably false); the Introduction
  reinstated the "self-contained" claim the ledger's own round-2 nitpick N6 had
  retired; and compressing ledger row S8 out of the paper broke Lemma 3's interface
  (its proof used col B = col A, which no printed statement supplied) and left a
  dangling Outlook pointer.
- **Repairs**: all 13 findings fixed in one pass — including strengthening
  Lemma 1's statement with col B = col A (its proof already establishes it), which
  repairs Lemma 3's interface and the Outlook pointer together. Page count 3 → 4.
- **Round 2 (re-audit)**: 12 of 13 repairs VERIFIED-FIXED; **gate 6 PASS**
  (abstract 155 words, all riders traveling, every restatement weaker-or-equal);
  gate 5(d) still FAIL, narrowly, on two §Verification sentences whose numbers did
  not match the deposited scripts (rows S5–S9/S14 unscreened by the universal claim;
  "thousands" resting on the undeposited 4,000-instance referee screen). Plus four
  low-priority precision notes (N-C/N-D/N-E/N-H) and the observation that the
  repository pointer should be confirmed at release (N-F — it is the repo's actual
  origin remote, verified in round 1 via `git remote -v`).
- **Round 3 (targeted re-run on the two rewritten sentences)**: numbers verified
  against the artifacts item by item (400 chain replay / 200 oblique rerun / 500
  trace-commutation pairs incl. n = 0, r = 0 / degenerate identities from the probe /
  1,100 = 400+200+500 exactly / 4,000 correctly attributed as reported-not-deposited);
  N-C/N-D/N-E/N-H all landed; no new violations; statements byte-identical.
  **Gate 5(d): PASS. Gate 6: PASS.**

Protocol note (failure discipline): the gate was re-run after the fix; no known
defect carried forward.
